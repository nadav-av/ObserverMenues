using System;

namespace Ex04.Menus.Test
{


    // $G$ SFN-004 (-5) You should display the type of menu (interface or delegate) each time we are at the main menu.

    // $G$ CSS-999 (-2) The Class must have an access modifier.
    class Program
    {
        public static void Main()
        {
            MenusRunner.Run();
        }
    }
}
